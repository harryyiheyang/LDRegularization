% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poet_block.R
\name{poet_block}
\alias{poet_block}
\title{Block-wise POET for LD matrix regularization}
\usage{
poet_block(
  R,
  n = NULL,
  regularizer = c("thresholding", "tapering", "linear_shrinkage", "nonlinear_shrinkage"),
  X = NULL,
  r_threshold = 0.95,
  cumvar = 0.95,
  K_min = 5,
  n_iter = 3,
  linkage = "complete",
  eigenmin = 0.001,
  lambda = NULL,
  K = NULL,
  alpha = NULL
)
}
\arguments{
\item{R}{Symmetric LD correlation or covariance matrix.}

\item{n}{Optional sample size. Used for theory-rate defaults when available.}

\item{regularizer}{Residual regularization method. One of
`"thresholding"`, `"tapering"`, `"linear_shrinkage"`, or
`"nonlinear_shrinkage"`.}

\item{X}{Optional individual-level data matrix. Required when
`regularizer = "nonlinear_shrinkage"`.}

\item{r_threshold}{Minimum pairwise absolute correlation used by the
fallback clustering cut. Default is 0.95.}

\item{cumvar}{Per-cluster cumulative variance proportion for eigenvector
retention. Default is 0.95.}

\item{K_min}{Minimum number of clusters. Capped at `ncol(R)`.}

\item{n_iter}{Number of subspace iteration steps. Default is 3.}

\item{linkage}{Hierarchical clustering linkage method. Default is
`"complete"`.}

\item{eigenmin}{Minimum eigenvalue target for positive-definite correction.
Default is 0.001.}

\item{lambda}{Optional threshold for `regularizer = "thresholding"`.}

\item{K}{Optional bandwidth for `regularizer = "tapering"`.}

\item{alpha}{Optional shrinkage intensity for
`regularizer = "linear_shrinkage"`, or smoothness parameter for the
default bandwidth in tapering.}
}
\value{
A list with components `R_hat`, `H`, `Lambda`, `E_hat`, `clusters`,
  `r_per_cluster`, `n_clusters`, `regularizer`, and `tuning`.
}
\description{
Regularize an LD correlation matrix by clustering variants on absolute
correlation, building a block-wise POET factor space, refining that space
with a few subspace iterations, and regularizing the residual with one of
four simple methods.
}
