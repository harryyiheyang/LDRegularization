% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariance_tl.R
\name{select_tl_alpha}
\alias{select_tl_alpha}
\title{Select transfer strength for covariance transfer learning}
\usage{
select_tl_alpha(
  X_target,
  source = NULL,
  R_source = NULL,
  X_source = NULL,
  P_source = NULL,
  strategy = c("covariance", "projection", "P"),
  method = c("thresholding", "banding", "tapering", "linear_shrinkage",
    "nonlinear_shrinkage"),
  tl_alpha_grid = c(0, 0.05, 0.1, 0.25, 0.5, 1, 2),
  folds = 5,
  lambda = NULL,
  K = NULL,
  alpha = 1,
  shrink_alpha = 0.5,
  nonlinear_shrinkage = 0,
  eigenmin = 0.001,
  source_rank = NULL
)
}
\arguments{
\item{X_target}{Target individual-level data matrix with observations in rows.}

\item{source}{Source information passed to `covariance_tl()`.}

\item{R_source}{Optional source covariance or correlation matrix.}

\item{X_source}{Optional source individual-level data. If `source` and
`R_source` are omitted, `R_source = matrixCor(X_source)` is used.}

\item{P_source}{Optional source basis or projection matrix for
`strategy = "projection"`.}

\item{strategy}{Source transfer strategy: `"covariance"` or `"projection"`.
`"P"` is accepted as an alias for `"projection"`.}

\item{method}{Residual regularization method.}

\item{tl_alpha_grid}{Candidate nonnegative transfer strengths. `0` is added
automatically if not supplied.}

\item{folds}{Number of folds. Default is 5.}

\item{lambda}{Optional threshold for `method = "thresholding"`.}

\item{K}{Optional bandwidth for `method = "banding"` or `"tapering"`.}

\item{alpha}{Smoothness parameter used only for default `K`.}

\item{shrink_alpha}{Shrinkage intensity used only for
`method = "linear_shrinkage"`.}

\item{nonlinear_shrinkage}{Mixing weight used only for
`method = "nonlinear_shrinkage"`. Default is 0.}

\item{eigenmin}{Minimum eigenvalue target for FSPD. Default is 0.001.}

\item{source_rank}{Optional rank used when `strategy = "projection"` and
`source` is a square covariance matrix.}
}
\value{
A list with the selected `tl_alpha`, average CV scores, fold scores,
  and the fold assignments.
}
\description{
Select `tl_alpha` by K-fold cross-validation on target individual-level data.
The validation score is the off-diagonal Frobenius loss on correlation scale.
}
