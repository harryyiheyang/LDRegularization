% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covariance_tl.R
\name{covariance_tl}
\alias{covariance_tl}
\title{Covariance transfer learning regularization}
\usage{
covariance_tl(
  S,
  source,
  n,
  strategy = c("covariance", "projection", "P"),
  method = c("thresholding", "banding", "tapering", "linear_shrinkage"),
  tl_alpha = 0.5,
  lambda = NULL,
  K = NULL,
  alpha = 1,
  shrink_alpha = 0.5,
  eigenmin = 0.001,
  source_rank = NULL
)
}
\arguments{
\item{S}{Target covariance or correlation matrix.}

\item{source}{Source information. For `strategy = "covariance"`, this must
be a covariance or correlation matrix. For `strategy = "projection"`, this
can be a source basis matrix with `p` rows, a source projector, or a source
covariance matrix from which a basis is extracted.}

\item{n}{Target sample size used to form `S`.}

\item{strategy}{Source transfer strategy: `"covariance"` or `"projection"`.
`"P"` is accepted as an alias for `"projection"`.}

\item{method}{Residual regularization method.}

\item{tl_alpha}{Nonnegative transfer strength. `0` means no transfer.}

\item{lambda}{Optional threshold for `method = "thresholding"`.}

\item{K}{Optional bandwidth for `method = "banding"` or `"tapering"`.}

\item{alpha}{Smoothness parameter used only for default `K`.}

\item{shrink_alpha}{Shrinkage intensity used only for
`method = "linear_shrinkage"`.}

\item{eigenmin}{Minimum eigenvalue target for FSPD. Default is 0.001.}

\item{source_rank}{Optional rank used when `strategy = "projection"` and
`source` is a square covariance matrix.}
}
\value{
A positive-definite transfer-learning regularized correlation
  matrix.
}
\description{
Use source covariance information to guide the factor space, then regularize
the target idiosyncratic component with one of the existing sparse methods.
}
